namespace Notio.Packets.Enums;

/// <summary>
/// Defines the priority levels of a packet.
/// </summary>
public enum PacketPriority : byte
{
    Low = 0,
    Medium = 1,
    High = 2,
    Urgent = 3
}