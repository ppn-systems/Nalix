<UserControl x:Class="Nalix.SDK.Tools.Views.HexViewerOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource OverlayBrush}"
            Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
        <Grid>
            <Border Width="720"
                    MaxHeight="520"
                    Padding="18"
                    Background="{DynamicResource SurfaceBrush}"
                    CornerRadius="12"
                    BorderBrush="{DynamicResource BorderStrongBrush}"
                    BorderThickness="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock FontSize="22"
                                   FontWeight="SemiBold"
                                   Text="{Binding Title}" />
                        <TextBlock Margin="0,6,0,0"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   Text="{Binding Source={StaticResource ToolTexts}, Path=HexViewerHint}" />
                    </StackPanel>

                    <TextBox Grid.Row="1"
                             Padding="12"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             TextWrapping="NoWrap"
                             FontFamily="Consolas"
                             IsReadOnly="True"
                             Text="{Binding Hex, Mode=OneWay}" />

                    <StackPanel Grid.Row="2"
                                Margin="0,14,0,0"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button Width="100"
                                Margin="0,0,10,0"
                                Command="{Binding CopyCommand}"
                                Content="{Binding Source={StaticResource ToolTexts}, Path=ButtonCopy}" />
                        <Button Width="100"
                                Command="{Binding CloseCommand}"
                                Content="{Binding Source={StaticResource ToolTexts}, Path=ButtonClose}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
