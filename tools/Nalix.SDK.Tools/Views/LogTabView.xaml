<UserControl x:Class="Nalix.SDK.Tools.Views.LogTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380" />
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <GroupBox Header="{Binding Source={StaticResource ToolTexts}, Path=GroupLogEntries}">
            <DockPanel Margin="10">
                <Button DockPanel.Dock="Bottom"
                        Margin="0,10,0,0"
                        Command="{Binding ClearCommand}"
                        Content="{Binding Source={StaticResource ToolTexts}, Path=ButtonClearLog}" />
                <ListBox DisplayMemberPath="Summary"
                         ItemsSource="{Binding Entries}"
                         SelectedItem="{Binding SelectedEntry}" />
            </DockPanel>
        </GroupBox>

        <Border Grid.Column="2"
                Padding="16"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="10"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock FontSize="22"
                           FontWeight="SemiBold"
                           Text="{Binding Source={StaticResource ToolTexts}, Path=LogDetailsTitle}" />

                <StackPanel Grid.Row="1" Margin="0,12,0,10">
                    <TextBlock>
                        <Run FontWeight="SemiBold" Text="{Binding Source={StaticResource ToolTexts}, Path=LogTimestampLabel}" />
                        <Run Text=": " />
                        <Run Text="{Binding SelectedTimestampText, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock Margin="0,6,0,0">
                        <Run FontWeight="SemiBold" Text="{Binding Source={StaticResource ToolTexts}, Path=LogSourceLabel}" />
                        <Run Text=": " />
                        <Run Text="{Binding SelectedSourceText, Mode=OneWay}" />
                    </TextBlock>
                </StackPanel>

                <TextBlock Grid.Row="2"
                           Margin="0,0,0,8"
                           FontWeight="SemiBold"
                           Text="{Binding Source={StaticResource ToolTexts}, Path=LogMessageLabel}" />

                <Border Grid.Row="3"
                        Padding="12"
                        Background="{DynamicResource SurfaceAltBrush}"
                        CornerRadius="8"
                        BorderBrush="{DynamicResource BorderStrongBrush}"
                        BorderThickness="1">
                    <TextBox IsReadOnly="True"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{DynamicResource SecondaryTextBrush}"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled"
                             Text="{Binding SelectedMessageText, Mode=OneWay}" />
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
