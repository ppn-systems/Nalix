<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<Nullable>enable</Nullable>
		<IsPackable>true</IsPackable>
		<OutputType>Library</OutputType>
		<IsTrimmable>false</IsTrimmable>
		<Deterministic>true</Deterministic>

		<!-- Versions -->
		<Version>10.0.0</Version>
		<FileVersion>10.0.0.0</FileVersion>
		<PackageVersion>10.0.0</PackageVersion>
		<InformationalVersion>10.0.0+$(SourceRevisionId)</InformationalVersion>

		<Description>
Nalix.Logging is a high-performance, asynchronous logging subsystem designed
for large-scale and low-latency applications in the Nalix ecosystem.

It features a modular engine with pluggable sinks (console, file, email, channel),
batched file writing, and fully configurable logging options.
Built with a zero-lock, channel-based pipeline, it ensures efficient,
non-blocking log processing under heavy concurrent workloads.
		</Description>

		<PackageReleaseNotes>
			- Introduced LogEngine with asynchronous channel-based batching pipeline.
			- Added multiple sinks: ConsoleLogTarget, FileLogTarget, EmailLogTarget, and ChannelFileLogTarget.
			- Implemented BatchFileLogTarget for high-throughput file I/O with low contention.
			- Added flexible options (FileLogOptions, BatchFileLogOptions, EmailLogOptions) for configuration tuning.
			- Integrated LogFormatter and LogDistributor for extensible message routing.
			- Added NLogix facade for simplified static logging entry points and extensions.
			- Improved async handling, internal exception safety, and batching performance.
			- General cleanup, documentation updates, and unit test enhancements.
		</PackageReleaseNotes>

		<AllowUnsafeBlocks>true</AllowUnsafeBlocks>

	</PropertyGroup>

	<Import Project="..\Nalix.Build.props" />
	<Import Project="..\Package.Build.props" />

	<ItemGroup>
		<ProjectReference Include="..\Nalix.Common\Nalix.Common.csproj" />
		<ProjectReference Include="..\Nalix.Framework\Nalix.Framework.csproj" />
	</ItemGroup>

</Project>
