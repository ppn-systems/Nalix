<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<Nullable>enable</Nullable>
		<OutputType>Library</OutputType>

    <Version>0.8.98</Version>
    <PackageVersion>0.8.98</PackageVersion>

    <Description>
Nalix.Network.Web is a high-performance, lightweight web framework for .NET, designed for building modular and extensible web servers. It supports authentication, CORS, routing, sessions, WebSockets, and security features.
    </Description>

    <PackageReleaseNotes>
      - Initial release of Nalix.Network.Web.
      - Features include:
      • Modular web server architecture with extensibility.
      • Authentication, CORS, and security middleware.
      • Routing and session management.
      • WebSockets support for real-time communication.
      • Built-in MIME type handling and HTTP utilities.
    </PackageReleaseNotes>
  </PropertyGroup>

	<Import Project="..\Nalix.Build.props" />
  <Import Project="..\Package.Build.props" />

	<ItemGroup>
    <ProjectReference Include="..\Nalix\Nalix.csproj" />
    <ProjectReference Include="..\Nalix.Shared\Nalix.Shared.csproj" />
	</ItemGroup>
  
</Project>
