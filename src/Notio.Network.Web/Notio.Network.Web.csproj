<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<Nullable>enable</Nullable>
		<OutputType>Library</OutputType>

    <Version>0.4.71</Version>
    <PackageVersion>0.4.71</PackageVersion>

    <Description>
      Notio.Network.Web is a high-performance, lightweight web framework for .NET,
      designed for building modular and extensible web servers. It supports
      authentication, CORS, routing, sessions, WebSockets, and security features.
    </Description>

    <PackageReleaseNotes>
      - Initial release of Notio.Network.Web.
      - Features include:
      • Modular web server architecture with extensibility.
      • Authentication, CORS, and security middleware.
      • Routing and session management.
      • WebSockets support for real-time communication.
      • Built-in MIME type handling and HTTP utilities.
    </PackageReleaseNotes>
  </PropertyGroup>

	<Import Project="..\Notio.Build.props" />
  <Import Project="..\Package.Build.props" />

	<ItemGroup>
    <ProjectReference Include="..\Notio\Notio.csproj" />
    <ProjectReference Include="..\Notio.Shared\Notio.Shared.csproj" />
	</ItemGroup>
  
</Project>
