<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<Nullable>enable</Nullable>
		<IsPackable>true</IsPackable>
		<IsTrimmable>false</IsTrimmable>
		<OutputType>Library</OutputType>
		<Deterministic>true</Deterministic>
		<AllowUnsafeBlocks>true</AllowUnsafeBlocks>
		<DisableImplicitMicrosoftCSharpReference>true</DisableImplicitMicrosoftCSharpReference>

		<!-- Versions -->
		<Version>7.7.2</Version>
		<FileVersion>7.7.2.0</FileVersion>
		<PackageVersion>7.7.2</PackageVersion>
		<InformationalVersion>7.7.2+$(SourceRevisionId)</InformationalVersion>

		<Description>
Nalix.Network is the high-performance, modular networking runtime for the Nalix ecosystem.
It provides TCP and UDP listener frameworks, connection hubs, middleware pipelines,
protocol lifecycle management, and adaptive throttling for large-scale distributed systems.

Built for real-time workloads, it emphasizes zero-allocation I/O, low-latency dispatch,
and precise timing synchronization — delivering reliable and efficient communication
between clients and servers under heavy concurrency.
		</Description>

		<PackageReleaseNotes>
			- Introduced TCP and UDP listeners with async event-driven connection handling.
			- Added Connection hub and transmission extensions with high-throughput batching.
			- Implemented middleware pipeline supporting inbound/outbound interceptors.
			- Added Protocol subsystem for lifecycle, metrics, and public RPC methods.
			- Introduced adaptive throttling: ConnectionLimiter, TokenBucketLimiter, and PolicyRateLimiter.
			- Added Timing subsystem with high-resolution TimingWheel and TimeSynchronizer.
			- Enhanced internal abstractions for extensibility and concurrent dispatch safety.
			- Major refactors for performance, stability, and maintainability across all layers.
		</PackageReleaseNotes>

	</PropertyGroup>

	<Import Project="..\Nalix.Build.props" />
	<Import Project="..\Package.Build.props" />

	<ItemGroup>
		<ProjectReference Include="..\Nalix.Common\Nalix.Common.csproj" />
		<ProjectReference Include="..\Nalix.Shared\Nalix.Shared.csproj" />
		<ProjectReference Include="..\Nalix.Framework\Nalix.Framework.csproj" />
	</ItemGroup>

</Project>
