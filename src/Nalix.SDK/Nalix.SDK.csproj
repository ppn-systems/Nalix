<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<IsPackable>true</IsPackable>
		<IsTrimmable>false</IsTrimmable>
		<OutputType>Library</OutputType>
		<Deterministic>true</Deterministic>

		<!-- NativeAOT: export UnmanagedCallersOnly symbols as a real DLL/.so/.dylib -->
		<PublishAot>true</PublishAot>
		<StripSymbols>true</StripSymbols>
		<Optimize>true</Optimize>
		<InvariantGlobalization>true</InvariantGlobalization>

		<!-- Reduce reflection metadata; keep it small & fast for interop -->
		<IlcGenerateCompleteTypeMetadata>false</IlcGenerateCompleteTypeMetadata>
		<IlcOptimizationPreference>Speed</IlcOptimizationPreference>

		<!-- Versions -->
		<Version>7.0.0</Version>
		<FileVersion>7.0.0.0</FileVersion>
		<PackageVersion>7.0.0</PackageVersion>
		<InformationalVersion>7.0.0+$(SourceRevisionId)</InformationalVersion>

		<Description>
Nalix.SDK provides the client-side development kit for the Nalix ecosystem,
offering high-level APIs, controller abstractions, time synchronization helpers,
and localization (L10N) utilities for building distributed applications.

It acts as the bridge between application logic and the Nalix.Network layer,
simplifying remote communication, configuration, and message orchestration
through clean, extensible, and developer-friendly interfaces.
		</Description>

		<PackageReleaseNotes>
			- Introduced Controller and DirectiveController for structured client-side orchestration.
			- Added Remote module with configurable reliable and unreliable communication clients.
			- Implemented TimeSyncExtensions for clock alignment between client and server.
			- Added L10N subsystem with Localization, Localizer, and MultiLocalizer support.
			- Improved configuration and extension APIs for SDK customization.
			- Enhanced dependency structure for modular SDK integration.
			- General cleanup, documentation updates, and performance tuning.
		</PackageReleaseNotes>

		<AllowUnsafeBlocks>true</AllowUnsafeBlocks>

	</PropertyGroup>

	<Import Project="..\Nalix.Build.props" />
	<Import Project="..\Package.Build.props" />

	<ItemGroup>
		<ProjectReference Include="..\Nalix.Common\Nalix.Common.csproj" />
		<ProjectReference Include="..\Nalix.Shared\Nalix.Shared.csproj" />
	</ItemGroup>

</Project>
